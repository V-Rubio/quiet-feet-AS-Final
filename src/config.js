var port = 3000;
var siteURL = 'http://localhost:' + port;

var dbPort = 3001; 
export var backendURL = 'http://localhost:' + dbPort;
// not changed in index.js

// export default siteURL;

// export default dbPort; - don't do this, it overrites all other variables
